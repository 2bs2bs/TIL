# v-slot

## なぜ使うか
親子コンポーネント間のやりとりでHTMLを表示できるようにするために使用。
親子のデータのやり取りではHTMLを渡せないため。

## 使い方
子コンポーネントで表示したい位置に`<slot/>`を用意
```
⋮
<slot/>
⋮
```
親コンポーネント子コンポーネントタグの中でで渡したいHTMLを記載
```
<子コンポーネント
  v-bind:name="name"
>
  <p>渡したいHTML</p>
</子コンポーネント>
```
スロットの部分に渡されるよという流れ